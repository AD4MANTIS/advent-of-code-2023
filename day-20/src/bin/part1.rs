#![allow(unused)]

// lib::day!(20, part1, example => 11687500);

fn main() {}

enum Module {
    // %
    FlipFlop(bool),

    // &
    Conjunction(bool),

    // broadcaster
    Broadcaster,
}

const HIGH: bool = true;
const LOW: bool = false;

fn part1(input: &str) -> usize {
    todo!()
}
